{
  "name": "tahoiya-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "build": "pnpm -r build",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@types/node": "^20.10.6",
    "concurrently": "^8.2.2",
    "lefthook": "^2.0.12",
    "typescript": "^5.9.3"
  }
}
